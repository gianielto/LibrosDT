name:CI
on: 
  push:
    branches:
      -main
      -feature/CI
      
     
  pull_request:
    branches:
      -main
      -feature/CI
     
jobs:
  tests: 
    steps: 
      - name: Descargar repositorio
        uses: actions/checkout@v4
      - name: instalar Node JS
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: verificar Node 
        run: node -v

